Imports System.Data.SqlClient

Public Class adminMain
    Dim ConStr As String = "Server=localhost; Uid=eslemus; database=cinema_db; Uid=root; Pwd=zero00"
    Dim con As SqlConnection = New SqlConnection
    Dim DataSetAccounts As New DataSet

    'Dim DataAdapter1 As MySqlDataAdapter


End Class